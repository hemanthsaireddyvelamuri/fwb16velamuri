extends layout

block content
  h1= title
  p Welcome to #{title}. Don't #{title} the #{title} untill you #{title} the #{title}.
  button(type="button", class="btn  btn-secondary" onclick="update()") Press Me for the contents
  p

  div(id ="target") Display

  script.
    var client_count=0;
    function update(){
      client_count+=1;
      
      document.getElementById("target").innerHTML="Value Now "+client_count  ;
      }


  p
  a(href="gas") Link to gas Page
  
    