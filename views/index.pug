extends layout

block content
  h1= title
  p Welcome to #{title}. Don't #{title} the #{title} untill you #{title} the #{title}.
  button(type="button", class="btn btn-secondary") Press Me for the contents.