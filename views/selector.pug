extends layout

block content
  h1= title
  - var image_names= ["gas1.jpg","gas2.jpg","gas3.jpg","gas4.jpg","gas5.jpg"]
  - for(let i=0;i<3;i++){
     - let randValue= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[randValue]}` alt=`Image No ${randValue}`)

  - }
    